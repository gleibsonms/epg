name: 🇧🇷 Generate Brazilian EPG

on:
  schedule:
    - cron: '0 2 * * *'  # executa todo dia às 02:00 UTC (~23:00 horário de Brasília)
  workflow_dispatch:  # permite rodar manualmente

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repositório
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: ⚙️ Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: 🧠 Gerar EPG (Brasil)
        env:
          M3U_URL: ${{ secrets.M3U_URL }}
          EPG_URL: ${{ secrets.EPG_URL }}
        run: |
          echo "Baixando lista M3U e gerando EPG com foco em canais brasileiros..."
          python epg_generator.py --m3u "$M3U_URL" --epg "$EPG_URL" --out epg.xml --hours 72 --tz-offset -3

      - name: 💾 Commit e enviar epg.xml
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add epg.xml
          git commit -m "🔁 Atualiza EPG automático (Brasil)" || echo "Sem alterações para commitar"
          git push
